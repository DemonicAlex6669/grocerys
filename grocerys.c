#include <stdio.h>

int main (void)
{
arr[] = {};

	if (arr != '/0')
	{
		char y/n;

		toupper(y/n);

		getf(y/n);

		file *fpt = fopen("groceries.txt", "a");
			if (fpt == NULL)
			{
			printf("failed to make file");
			return 1;
			}

			if (strcmp(y/n, 'Y') == 0)
			{
			int i = 0;

			fprint(fpt, "%s", arr[i]);

			i++;
			}
			else if (strcmp(y/n, 'N') == 0)
			{
			i++;
			}
	}
fclose(ftp);
}
