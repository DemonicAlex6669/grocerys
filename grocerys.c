#include <stdio.h>

int main (void)
{
arr[] = {};
int arrleng = sizeof arr/ sizeof arr[0];

	if (arr != '/0')
	{
		char y/n;

		toupper(y/n);

		getf(y/n);

		file *fpt = fopen("groceries.txt", "a");
			if (fpt == NULL)
			{
			printf("failed to make file");
			return 1;
			}
			for (i = 0, i < arrleng, i++)
			{
				if (strcmp(y/n, 'Y') == 0)
				{
				int i = 0;

				fprint(fpt, "%s", arr[i]);
				}
			}
	}
fclose(ftp);
}
