#include <stdio.h>

int main (void)
{
arr[] = {};

	if (arr != '/0')
	{
		char y/n;

		toupper(y/n);

		getf(y/n);

			if (strcmp(y/n, 'Y') == 0)
			{
			.
			}
			else if (strcmp(y/n, 'N') == 0)
			{
			.
			}
	}
}
